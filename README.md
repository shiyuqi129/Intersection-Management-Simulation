# Intersection-Management-Simulation

### Requirements:
`python>=3.11`<br>
`networkx>=3.6.1`<br>
`pygame>=2.6.1`

### Warning:
There is a lot of bugs in the current stage and it does not work properly